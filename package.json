{
  "name": "tinasaurus",
  "version": "0.0.0",
  "scripts": {
    "docusaurus": "docusaurus",
    "dev": "tinacms dev --noTelemetry -w docs,blog,src/pages,config --datalayer-port 4002 -c \"docusaurus start --port 3001\" ",
    "dev:reset": "rm -rf .tina && tinacms dev --noTelemetry -w docs,blog,src/pages,config --datalayer-port 4002 -c \"docusaurus start --port 3001\" ",
    "start": "docusaurus start",
    "build": "tinacms build && docusaurus build",
    "build-local": "tinacms build --local --skip-indexing --skip-cloud-checks && docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "^3.8.1",
    "@docusaurus/preset-classic": "^3.8.1",
    "@mdx-js/react": "^3.1.0",
    "@tinacms/cli": "^1.10.0",
    "clsx": "^2.1.1",
    "prism-react-renderer": "^2.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "scheduler": "^0.23.2",
    "tinacms": "^2.8.0",
    "title": "^3.5.3"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.8.1"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
